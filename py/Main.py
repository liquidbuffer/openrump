import open_rump
import Hello

if __name__ == '__main__':
    # game = open_rump.Game()
    # cam = game.create_camera('camera')
    # player = game.load_player('player', 'twilightsparkle.mesh')
    # game.attach_camera_to_entity(cam, player)
    # game.get_world().get_system("CameraOrbit")
    #test = Test(game)
    #game.run()
    test = Hello.Test()
    test.test()